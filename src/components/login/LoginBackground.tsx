import React from "react";

export const LoginBackground = () => {
  // This component is no longer needed as the background is now handled directly in Login.tsx
  // Keeping it empty to maintain component structure
  return null;
};
